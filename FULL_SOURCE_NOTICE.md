# تنبيه بخصوص مصدر المشروع الكامل

مرجع آخر أرشيف أصلي معروف يحمل البصمة:

```text
58130f9529f6f362717ca4993002c052bf7252ecb5ff62655fd2618c9c0f300d
```

لم تكن Bytes الأرشيف الأصلي متاحة داخل مساحة الملفات النشطة أثناء هذا العمل؛ لذلك الملف المرفق هو **حزمة دمج كاملة** وليس ادعاءً بأنها نسخة byte-for-byte من مشروعك الأصلي.

الحزمة تحتوي على جميع ملفات الميزة، الترحيل، الواجهة، Docker، الاختبارات، الوثائق، وسكربت دمج لا يستبدل ملفات الدخول الرئيسية استبدالًا أعمى. كما تحتوي على سكربت يبني ZIP مصدرًا كاملًا حقيقيًا بمجرد تزويده بأرشيفك الأصلي:

```bash
python scripts/build_full_source_from_original.py BASE.zip OUTPUT.zip
```

المسار الافتراضي يفرض التطابق الدقيق مع SHA-256 أعلاه. خيار
`--allow-different-base` تجاوز يدوي **غير آمن** لإصدار مختلف، ولا يعمل إلا مع
`--acknowledge-unverified-base` أيضًا: يتطلب مراجعة الفروقات
ولا يعطل الفحص التمهيدي الكامل أو Verifier، لكن نجاحهما لا يثبت مطابقة خط الأساس
ولا يجعل الناتج Verified أو مصدرًا كاملًا حقيقيًا متحققًا. يرفض Builder تطابق مساري
المصدر والناتج، ويرفض الناتج الموجود دائمًا؛ يجب اختيار مسار جديد أو إزالة القديم
يدويًا قبل البدء، ولا يوجد خيار overwrite. يبني الناتج في حجر
خاص تحت قفل إخراج حصري. يُبصم المصدر ويُفك من المقبض الثابت نفسه، ويعاد فحص هويته
والبصمة قبل الفك وبعده. تُغلف Snapshot مثبتة للملفات من مقابض no-follow مع فحص
الهوية والـHash قبل/بعد القراءة وMetadata مضبوطة. يعاد Hash الحجر مباشرة قبل نشر
no-clobber فقط، ثم يعاد Hash الناتج النهائي. لا توجد استعادة لناتج سابق.
كما يرفض الدمج أي اختلاف نصي
في قيد حزمة موجودة في ملفي requirements ويطلب قيدًا موحدًا يدويًا بدل الاعتماد على
تثبيت Docker ثانٍ. لا يستثني Verifier أي Backup داخل الشجرة.
