# Structural Preflight and External Gate Status

**Date:** 2026-08-02  
**Release status:** **BLOCKED / NOT DEPLOYABLE**

## Current Verification

- The previously reported **52 passed, 1 skipped** pytest result predates the latest static-only edits in this remediation pass. A rerun is explicitly prohibited for this edit, so that result is historical and does not verify the current files.
- The previous Python `compileall` result also predates the latest edits. No Python or project command was imported, parsed, compiled, linted, tested, or executed during this pass.
- The source-independent backend has **static signoff with no known actionable critical/high backend defect** in the current files. This is a static-only assessment; it is not a build, import, test, or runtime certification, and it does not establish that the backend matches the original source.
- Static inspection of canonical `docker-compose.yml` shows **16 services**, the exact six-network membership matrix, and the exact nine-volume mount matrix. The dispatcher now mounts only `formatting-execution-exchange` for janitor access and still receives no OpenCode auth. `data`, `formatting-control`, and API-facing `gateway` are internal; public `edge` and the two role-specific egress networks are explicitly non-internal. Gateway IPAM/static-address, outer-proxy sanitation including XFP, and read-only stock-Nginx compatibility with Coolify have not been run.
- Static verifier source now applies the forbidden runtime-override matrix to every service, permits root and `network_mode: none` only on the two init services, preserves only the exact canonical gateway capability additions, rejects host/bind mounts, and requires the canonical hardening fields where defined. It also requires all nine top-level named-volume definitions to be empty/null. Mutation tests were added statically but were not run.
- The deployment verifier is **structural only and cannot establish external trust in its own files**: it proves fail-closed conformance of the current tree to the canonical static contracts, not authenticity, completeness, buildability, or runtime safety. External trust requires the verified original ZIP plus independent build and runtime gates.
- Static file inspection found and removed the existing root `.pytest_cache` contents and the empty `tests/__pycache__` directory; no `.pyc` or `.pyo` files were found. The fail-closed release-tree scanner and `.dockerignore` continue to reject/exclude all four artifact classes.
- The verifier now requires exactly the nine canonical top-level Compose keys and rejects top-level YAML merge, `include`, `extends`, `configs`, `secrets`, and every other unexpected section. Every service rejects `post_start`, `pre_stop`, `configs`, `secrets`, and `extends`; `profiles`, `scale`, and `deploy` are entirely forbidden so all 16 required services remain enabled by default with one canonical instance. Static mutation tests cover zero, one, and divergent replica overrides but were not run.
- Docker is unavailable, so Compose runtime validation and image builds have not run.
- The expected original ZIP, SHA-256 `58130f9529f6f362717ca4993002c052bf7252ecb5ff62655fd2618c9c0f300d`, has not been supplied.
- `frontend/index.html`, `frontend/package.json`, a frontend lockfile, the original core backend modules, and the verified original archive are absent from this integration-only workspace. Frontend TypeScript compilation and complete application verification are therefore impossible here. The verifier requires the entry file, manifest, and at least one Dockerfile-supported lockfile; exact Vite/TypeScript config filenames are unknown from the unavailable original and are not guessed, so transitive completeness cannot be proved.
- The bundle migration intentionally still contains `REPLACE_WITH_CURRENT_HEAD`; it is a template that remains unresolved until integration against the missing original Alembic history.
- **INTEGRITY MANIFEST REFRESHED:** `SHA256SUMS.txt` was regenerated by the orchestrator via a non-Python (PowerShell SHA-256) pass over the current static-only tree using the exact deterministic format of `scripts/generate_sha256s.py` (66 regular files, ordinal sort, `./` relative POSIX paths, two-space separator, no headers). `scripts/generate_sha256s.py --check` must still be run in an execution-allowed context to confirm byte-for-byte parity. The manifest is authoritative only for this static-only tree snapshot; it cannot vouch for the final merged tree that will exist after the verified original ZIP is integrated.
- **NON-AUTHORITATIVE CHANGE LIST:** `CHANGED_FILES_MANIFEST.md` is an editorial inventory only. It must not be used as completeness evidence until regenerated from the approved final tree together with fresh checksums.

## Completed Repairs

The bundle now includes a canonical 16-service Compose stack; staged, rollback-preserving integration with a complete original-source preflight; lstat/reparse/special-object and resolved-containment gates for integration/build source and target trees; direct-apply baseline identity refusal by default; metadata-backed created-file recovery; recursive legacy-overlay rejection; a shared fail-closed release-tree policy for integration/build/verifier tooling; graph-validated Alembic integration; strict full-source secret/local-state rejection; corrected PostgreSQL claim ordering; separated public edge/internal gateway/data/control/role-egress networks; sanitized gateway forwarding and static gateway-only trust; a non-trusting backend image default; read-only/capability-limited Nginx; an all-service forbidden runtime-override matrix; empty/null-only named-volume definitions; structurally authenticated Redis; denied general Bash; verified-archive-only runtime skill materialization with repairable extracted cache; bounded and contained transcript snapshots; incremental cancellable workspace/output scans; strict artifact and DOCX validation; generation-fenced completion; persistent-parent containment; whole-workspace live quotas including pre-execution baseline checks; cumulative persistent-storage admission with active-job reservations; generation-specific execution and persistent result paths; UUID-fenced transactional Outbox dispatch; leases and stale-work recovery; deterministic repository tie-breakers; OAuth hardening; frontend workflow repairs; and exact verifier topology/dependency/health/secret/proxy gates. These are source-level implementations only. Transactional Outbox, lease behavior, archive materialization, path/quota enforcement, gateway static-IP/proxy/hardening behavior, and every current runtime change remain runtime-unverified.

The latest formatting-only static edit additionally requires a nonempty UTF-8
`output/result.md` and an exact successful `output/manifest.json`, admits storage
against both skills and the separately mounted jobs root, and reconciles interrupted
generation output/log publication from validated structure and hashes on the next
worker attempt. Hard-process-crash behavior remains runtime-unverified.

The current static-only integration/build edit removes output overwrite support and
its former CLI option entirely. The final path must be absent at start and
publication; the operator must choose a new path or remove the old output manually.
An atomically created adjacent output lock remains held through finalization and an
existing/stale lock is never auto-deleted. Packaging records the regular-file tree
snapshot, opens each source with a stable no-follow descriptor, streams its bytes into
controlled `ZipInfo` metadata, checks identity/hash around every archive read, and
rechecks the complete snapshot. Publication is atomic hard-link no-clobber only.

Integration now creates a separate adjacent project lock with `O_CREAT|O_EXCL` and
holds it across the final baseline-tree and per-target identity/hash rechecks, backup,
commit, and rollback. It rejects stale locks without deleting them. Every destination
is persisted in the attempted-destination inventory before replacement; the temporary
replacement identity/hash is registered before `os.replace`, and rollback touches a
destination only while it still matches that owned result. Concurrent edits cause a
fail-closed stop. No tests, parsers, hashes, or project commands were run.

## Finding Status

| Finding | Status | Current disposition |
|---|---|---|
| C-01 | **Blocked** | Complete original backend/frontend source is absent; the verified original ZIP is required. |
| C-02 | **Completed at source level; runtime-unverified** | Canonical Compose contains 16 services and six declared networks by static inspection. Runtime Compose and Coolify IPAM/static-address validation remain unexecuted. |
| C-03 | **Implemented at source level; runtime-unverified** | Integration lstat-checks every source/target ancestor, rejects link/reparse/special objects, holds an exclusive adjacent project lock over final tree/target rechecks through rollback, and restores only lock/identity-owned attempted destinations. Builder binds base hashing/extraction to one stable handle, streams a verified source snapshot from no-follow handles into controlled ZIP entries, holds an exclusive adjacent output lock, requires an absent final path, publishes by atomic no-clobber only, and rehashes final bytes. There is no overwrite or previous-output restoration path. Alternate ZIP still requires both unsafe flags. No current test or execution verifies these edits. |
| C-04 | **Implemented as structural preflight; external gates blocked** | The exact matrix requires context `.` and the role-specific backend, formatting, or frontend Dockerfile; locally built roles cannot be replaced by arbitrary images, and image-only roles must use their canonical image. Actual `docker compose config` and every image build remain mandatory and unexecuted. |
| C-05 | **Partially completed** | Alembic integration now parses both `down_revision` and `depends_on`, rejects malformed/ambiguous values, duplicate or reserved revision IDs, dangling references, combined-edge cycles, multiple `down_revision` heads, existing formatting integration, and later formatting descendants through either edge before writes. Head determination remains based only on `down_revision`, as Alembic requires. Exact placeholder rendering remains implemented, but the bundle template still has the unresolved placeholder by design; original migration history and real PostgreSQL upgrade/downgrade verification are unavailable. |
| H-01 | **Partially completed** | General Bash is denied, Redis is authenticated, API has only internal gateway/data/control networks, and public edge/role-egress networks are separated. Per-job ephemeral containers, single-workspace OS mounts, and isolation of OAuth credentials from execution are not implemented and remain blocked. |
| H-02 | **Implemented at source level; runtime-unverified** | Existing persistent parents and output/log targets are contained and checked for links, reparse points, hardlinks, and special files; artifact collection requires the explicit job/generation execution or persistent path; and live whole-workspace scans cover all entries. Before OpenCode, a persisted manifest records transcript/title/config hashes and sizes plus every selected-skill file's relative path/hash/size. Input/skill/policy files and directory identities are rechecked before any output validation on normal and exceptional returns, and POSIX read-only permissions protect those trees while output/log remain writable. OpenCode currently shares the UID and could theoretically chmod if it gained execution, although Bash is denied. No runtime proof is available. |
| H-03 | **Implemented at source level; runtime-unverified** | Transactional Outbox delivery now has an independent UUID claim fence: publish, invalid rejection, and failure release require matching event ID, `processing` status, and token; reclaim replaces the token and terminal/pending transitions clear it. Independent generation fencing, generation-fenced artifact completion, leases, stale running recovery, old queued reconciliation, bounded draining, and generation-specific workspaces are present. The repository exposes running generation keys with unexpired/recent leases; the dispatcher uses them to fence a bounded, sentinel-checked, grace-aged orphan-exchange janitor. PostgreSQL/Celery behavior and crash recovery have not run. |
| H-04 | **Completed** | The core API no longer depends on OpenCode health; only the formatting worker does. |
| H-05 | **Completed** | Default-skill replacement is selected and committed atomically when the current default is disabled. |
| H-06 | **Implemented at source level; runtime gate blocked** | Nginx exposes one prefix-locked `/api/` proxy contract, overwrites forwarded client identity with its immediate peer, strips incoming `Forwarded`, normalizes Host, denies hidden paths, and constrains scheme to exact `http`/`https`. Compose derives API-only `FORWARDED_ALLOW_IPS` from the static `GATEWAY_PEER_IP`; the backend image default disables proxy headers and configures no forwarded trust. Coolify IPAM/static-address support, outer-proxy sanitation including XFP, collision avoidance, and deployed behavior remain mandatory unexecuted staging gates. |
| H-07 | **Partially completed** | Provider identifiers/paths, OAuth URLs, explicit success values, and user-facing errors are hardened. The real OpenCode API contract and OAuth end-to-end flow have not been tested. |
| M-01 | **Completed at source level** | The root `.dockerignore` excludes environment/auth/cookie material, SSH/GPG/cloud/container credentials, private key/certificate stores, local databases/state, integration backups, caches, and artifacts while retaining explicit examples and application source patterns. |
| M-02 | **Implemented as structural preflight; runtime-unverified** | The preflight enforces the exact canonical top-level Compose key set, rejects top-level merge/external composition and top-level configs/secrets, enforces exact source/build/image/proxy matrices, rejects lifecycle/config/secret/profile/scaling/runtime overrides and host/bind mounts across every service, enforces canonical user/hardening exceptions, requires empty/null-only named-volume definitions, and scans the whole tree with shared rules including Python/pytest caches. Integration/build additionally reject links, reparse points, broken links, and special objects throughout source, target, extraction, nested-overlay, recovery, and package trees. Passing does not prove transitive source completeness, Compose validity, image buildability, or runtime behavior. No verifier or test was run in this static-only pass. |
| M-03 | **Completed** | Compose environments and credentials are split by service role, including one Deepgram key per transcription worker. |
| M-04 | **Implemented at source level; runtime-unverified** | One-shot services are excluded from health accounting, long-running roles have structurally and semantically checked health definitions, both OpenCode processes are supervised, and dispatcher health uses a fresh atomic application heartbeat. Scheduler health requires exactly Tini's `app.scheduler` child, but this is process liveness only until the original scheduler exposes a functional heartbeat. Exact storage/migration/PostgreSQL/Redis/OpenCode dependency conditions are statically enforced. |
| M-05 | **Implemented at source level; runtime-unverified** | CPU, memory, PID, log rotation, stop signals, grace periods, output/log limits, configurable whole-execution-workspace entry/byte enforcement, and finite cumulative persistent admission (`FORMATTING_STORAGE_MAX_BYTES`, 20 GiB default) are present. Admission scans incrementally, rejects links/special files, stops at the byte threshold, serializes creators, and reserves worst-case input/output/log growth for active and new jobs. Temporary exchange cleanup is separately interval/grace/lease-window/scan-bound configurable, restores read-only-tree permissions only after a safe scan, and warns while retaining unsafe or failed cleanup targets. Runtime enforcement has not been exercised. |
| M-06 | **Partially completed** | OpenCode and the preferred package-manager version are pinned and formatting dependencies are bounded. The four duplicates in `requirements-formatting.txt` exactly match canonical `requirements.txt` lines. Integration normalizes distribution names as text, accepts only exact equivalent overlaps, and fails differing target constraints with a manual merged-constraint instruction so Docker's later transaction cannot silently replace an original overlap. It intentionally does not implement a packaging resolver. Complete application lockfiles, dependency pins, and reproducible builds cannot be established without the original source. |
| M-07 | **Partially completed** | Backup guidance includes `formatting-jobs-data`, and artifact paths are relative to the configured root. No backup/restore drill has run. |
| M-08 | **Partially completed** | Completed source state, atomic cancellation, and cooperative worker checks with a single two-second DB throttle are implemented. Cancellation does not issue Celery revoke or a verified remote OpenCode session abort; terminal database state is authoritative. `source_job_id` still has no foreign key to the original jobs table. |
| M-09 | **Implemented at source level; runtime-unverified** | The worker depends only on the content-addressed archive under `skill_archives_root`, not the mutable extracted cache. It matches snapshot name/slug/SHA, reads and hashes the bounded archive immediately before use, centrally revalidates safe ZIP paths/types/count/unpacked size/exactly one `SKILL.md`/folder-name agreement, and materializes only the selected skill into the generation exchange. Re-upload can atomically repair a missing/corrupt extracted cache from the verified ZIP. Runtime proof under real multi-process/shared-volume concurrency is incomplete. |
| M-10 | **Completed** | Frontend source implements post-load initialization, busy/error states, active-rerun prevention, cancellation, and attempt history, with server-side single-flight enforcement. The frontend TypeScript build remains blocked by missing `package.json` and original source. |
| M-11 | **Implemented at source level; runtime-unverified** | TXT input must resolve beneath `EXPORTS_ROOT`, every existing component is checked, symlink/reparse/special/hardlink inputs are rejected, `FORMATTING_INPUT_MAX_BYTES` is checked before copying, hashing is streaming, and staged size/hash are rechecked. Canonical Compose now passes both `FORMATTING_INPUT_MAX_BYTES` and `FORMATTING_STORAGE_MAX_BYTES` to `api` and `formatting-worker`, with the exact recipients statically enforced. |

Formatting storage admission now scans `skills_root` and `jobs_root` separately,
without scanning their parent and double-counting the nested jobs volume. It keeps
active-job reservations and reserves uploaded archive bytes plus maximum unpacked
bytes before skill installation. This is admission, not retention.

Failed/cancelled database history and persistent input snapshots are retained, as
are already published generations. Completed history and artifacts have no
automatic deletion. The dispatcher is granted only the execution-exchange mount
for janitor work and no OpenCode authentication. It removes only valid stale
temporary generation directories after preserving the sentinel, DB-active/recent
lease keys, and grace-aged workspaces. Mounted cleanup behavior remains
runtime-unverified; completed/failed/cancelled persistent retention remains blocked
and no persistent formatting-retention policy is claimed here.

## Outstanding Acceptance Gates

The missing verified original source, unresolved migration placeholder in the bundle template, per-job container and single-workspace OS-mount isolation, a verified remote OpenCode session-abort mechanism, persistent formatting-retention cleanup (including documented failed/cancelled history), hard-crash reconciliation for interrupted persistent directory swaps, the source-job foreign key, signed external integrity evidence, PostgreSQL/Alembic and Celery crash integration testing, Docker builds, frontend TypeScript build, OpenCode contract/end-to-end testing, backup restore, and Coolify staging are **not complete**. Staging must specifically prove configurable gateway IPAM/static-address support, no subnet collision, retained peer address, Coolify sanitization of incoming forwarding headers, constrained forwarded scheme behavior, gateway-only public routing, and stock-Nginx startup under read-only/tmpfs/capability restrictions. All runtime verification is blocked and no current static implementation is certified by the historical test result.

Clarification for the latest edit: failed/cancelled retention is now documented,
and next-attempt interrupted-publication reconciliation is implemented at source
level. The outstanding gates are an intentional deletion/retention policy and
runtime crash verification, not absence of those two static changes.

Repository latest/list and recovery selection queries use deterministic `attempt_number`/`id` tie-breakers where timestamp semantics can tie. No remote OpenCode abort, retention cleanup, per-job container mounts, source FK, checksum regeneration, or runtime verification was attempted. The current static edit changed only integration/build tooling, text tests, operator/integration documentation, status, and the editorial changed-files manifest. No backend, Compose, verifier, Dockerfile, frontend, or checksum file was changed, and no Python/project/Docker/YAML command was imported, parsed, compiled, linted, tested, hashed, or run.

**Next action:** provide the local path to the verified original ZIP with SHA-256 `58130f9529f6f362717ca4993002c052bf7252ecb5ff62655fd2618c9c0f300d` (SHA `58130f...f300d`). Supplying that path unblocks the source/migration gates; after integration the integrity manifest must be regenerated again for the final merged tree; nothing is deployable until then.
