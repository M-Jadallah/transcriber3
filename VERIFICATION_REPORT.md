> **HISTORICAL VERIFICATION ONLY:** All commands and results in this report predate the current static-only edits and were not rerun. See [`REMEDIATION_EXECUTION_STATUS.md`](REMEDIATION_EXECUTION_STATUS.md) for current status.

# تقرير تاريخي لنتائج التحقق السابقة

**التاريخ:** 2 أغسطس 2026  
**النطاق:** نظام التنسيق المشتق لتطبيق YouTube → Deepgram.

## النتيجة

أبلغ التشغيل السابق عن نجاح **52 اختبارًا آليًا** وتجاوز اختبار واحد. لم تُشغّل الاختبارات أو Python بعد إصلاحات المراجعة الساكنة الحالية تنفيذًا لقيد التحرير الساكن. لا يمكن تشغيل بناء TypeScript أو Docker قبل دمج المصدر الأصلي الكامل.

الأداة الحالية فحص تمهيدي بنيوي فقط، وليست تحقق بناء أو نشر. نجاحها لا يغني عن
`docker compose config` الفعلي وبناء جميع الصور وبناء الواجهة والترحيلات واختبارات
Staging، ومنها تطهير XFP في Coolify. لم تُنفذ هذه البوابات في التعديل الحالي.

أضيفت نصيًا، من دون تشغيل، حالات Builder/Verifier/Integration لفشل Verifier قبل
التغليف، وبصمة ZIP الناتج، والتحذير غير الآمن للمصدر المختلف، ورفض الأسرار والحالة
المحلية دون حساسية لحالة الأحرف، والفشل على الأسماء المشتبهة، ورفض الـoverlay عند
أي عمق، وحواف Alembic `depends_on` المعلقة/الدورية/اللاحقة/الملتبسة، والحفاظ على
Backup خارج المشروع حتى نجاح Verifier والتغليف. لا تدخل هذه الحالات ضمن النتيجة
التاريخية أعلاه.

أضيفت في الدفعة الساكنة الأحدث اختبارات نصية أخرى لرفض الروابط وReparse Points
والملفات الخاصة، واحتواء المسارات المحلولة، ورفض التطبيق المباشر بلا هوية/إقرار،
وجرد الملفات المنشأة في Metadata، وترتيب الفحص ثم نشر ZIP ثم بصمة الناتج ثم تنظيف
الاسترداد، وحجر الناتج عند فشل الإنهاء. لم تُشغّل هذه الاختبارات ولم تُحسب أي بصمة.

أضيفت ساكنًا بعد ذلك حالات رفض تطابق مساري ZIP ورفض استبدال الناتج بلا خيار صريح،
والحفاظ على الناتج القديم عند فشل النشر أو المقاطعة، وترتيب الحجر ثم سلامة ZIP ثم
SHA ثم تنظيف الاسترداد والعمل ثم `os.replace` النهائي. كما أضيفت حالتا requirements
النصيتان: قبول التكرار المكافئ تمامًا، ورفض اختلاف قيد الاسم الموحّد مع طلب دمج
يدوي. لم تُشغّل هذه الاختبارات، ولم تُحسب SHA في هذه الدفعة.

أُلغي ذلك الخيار لاحقًا في التعديل الساكن الحالي: لا يقبل Builder ناتجًا موجودًا
إطلاقًا، ولا يحتوي خيار overwrite أو مسار استعادة لناتج سابق. أضيفت نصيًا
عقود قفل المشروع الحصري، وإعادة فحص هوية الشجرة والأهداف تحت القفل، وتسجيل الوجهة
قبل الاستبدال، وrollback واعٍ بالملكية، وتغليف Snapshot من مقابض no-follow بدل
`archive.write`. لم تُشغّل هذه الاختبارات ولم تُحسب أي SHA.

أضيفت ساكنًا في التعديل الحالي اختبارات نصية لهوية ملف المصدر من مقبض واحد قبل
الفك وبعده، وقفل الناتج الحصري، وإعادة Hash الحجر قبل النشر والناتج بعده، والنشر
no-clobber، والاستبدال/الاستعادة المحروسين بالهوية. كما ثُبتت أسطر الاعتماديات
المكررة في الملفين بالنص نفسه. لم تُشغّل الاختبارات ولم تُحسب أو تُجدد أي بصمة.

## التغطية الآلية

- قبول مهارة صحيحة في الجذر أو داخل مجلد مطابق للاسم.
- رفض الاسم غير المطابق لمجلد المهارة.
- التحقق من حقول YAML المعترف بها وخريطة `metadata` النصية.
- رفض Zip Slip والمسارات المطلقة والروابط الرمزية والمسارات المكررة.
- حدود حجم ZIP وعدد الملفات والحجم المفكوك.
- حفظ إصدار المهارة حسب SHA-256 وإعادة استخدام الإصدار نفسه.
- Snapshot مستقل للنص مع فحص بصمته وقت التنفيذ.
- فحص بصمة أرشيف المهارة قبل كل مهمة.
- نسخ المهارة المختارة وحدها إلى مساحة المهمة.
- رفض مخرجات تتجاوز العدد أو الحجم المسموح.
- رفض Symlink وHardlink والملفات الخاصة والامتدادات غير المسموح بها في المخرجات.
- فحص عدد ملفات DOCX الداخلية وحجمها بعد فك الضغط.
- منع Bash العام داخل سياسة OpenCode.
- منع مهام التنسيق النشطة المكررة، والإلغاء التعاوني، واستبدال المهارة الافتراضية بصورة ذرية.
- التحقق من مسارات OAuth ونتيجة نجاح المصادقة الصريحة.
- رفض DOCX تالف وفتح DOCX صالح فعليًا بواسطة `python-docx`.
- رفض JSON غير صالح وفحص رأس PDF.
- فصل حساب OpenCode وحذف ملف الاعتماد وجدولة إعادة التشغيل.
- فحص تمهيدي لسكربت الدمج قبل أي كتابة.
- اكتشاف رأس Alembic وربط الترحيل وإنشاء Backup.
- رفض تعارض Revision ID.
- رفض بنية واجهة غير متوقعة من دون ترك دمج جزئي.
- بناء ZIP مصدر كامل من أرشيف تجريبي، ورفض الأسرار والـBackup عند أي عمق.

## فحوص إضافية

- `python -m compileall` لجميع ملفات Backend والسكربتات والاختبارات.
- تحليل AST لجميع ملفات Python.
- تحليل نحوي لملفات `.ts` و`.tsx` باستخدام TypeScript 5.8.3.
- يُظهر الفحص الساكن الحالي لـ`docker-compose.yml` الموحد 16 خدمة؛ كانت قراءة PyYAML المذكورة أعلاه من تشغيل سابق.
- أضيف ساكنًا فصل شبكة `edge` العامة عن شبكة `gateway` الداخلية، وعزل خروج OpenCode وعمّال التفريغ، وحماية Redis بكلمة مرور؛ لم تُعد اختبارات هذه التغييرات.

## أمور لم تُنفّذ في البيئة الحالية

- بناء صور Docker وتشغيل `docker compose config`؛ Docker غير متوفر هنا.
- تشغيل PostgreSQL وRedis وتطبيق Alembic على قاعدة فعلية.
- ربط حساب ChatGPT الحقيقي أو تنفيذ طلب مدفوع.
- اختبار مهارتك الفعلية؛ ملف ZIP الحقيقي للمهارة لم يكن متاحًا في البيئة.
- دمج الحزمة وبناء الواجهة TypeScript؛ الأرشيف الأصلي نفسه لم يكن مركبًا وملفات `package.json` والتطبيق الأصلي غير موجودة هنا.

## فحص القبول على السيرفر

1. طبّق الحزمة على نسخة Git أو Backup من المشروع.
2. شغّل `alembic upgrade head` على قاعدة تجريبية أو Backup أولًا.
3. ابنِ `docker-compose.yml` الموحد بعد الدمج.
4. اربط ChatGPT من الإعدادات وتحقق من ظهور `openai` والنماذج.
5. ارفع المهارة الحقيقية.
6. نسّق تفريغًا قصيرًا مرتين وتأكد من ظهور محاولتين مستقلتين.
7. افتح `result.md` وDOCX الناتج فعليًا.
8. افصل الحساب وأعد ربطه.
